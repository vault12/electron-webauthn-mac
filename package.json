{
  "name": "electron-webauthn-mac",
  "version": "1.0.0",
  "description": "A native WebAuthn addon for Electron on macOS",
  "main": "js/index.js",
  "types": "js/index.d.ts",
  "gypfile": false,
  "scripts": {
    "clean": "rm -rf build build_swift native",
    "build": "node-gyp configure && node-gyp build && mkdir -p native && cp build/Release/webauthn_mac_addon.node native/",
    "rebuild": "npm run clean && npm run build",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vault12/electron-webauthn-mac.git"
  },
  "dependencies": {},
  "devDependencies": {
    "node-gyp": "^11.5.0",
    "node-addon-api": "^8.5.0",
    "electron-rebuild": "^3.2.9"
  },
  "files": [
    "js/**",
    "native/*.node"
  ]
}
